services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: liftlytics
      POSTGRES_PASSWORD: liftlytics
      POSTGRES_DB: liftlytics
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U liftlytics -d liftlytics"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
